name: Trigger JitPack API Build

on:
  push:
    branches:
      - main
    tags:
      - "*"
    paths:
      - "modules/api/**"
      - ".github/workflows/publish-api.yml"
      - "jitpack.yml"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Resolve JitPack target version
        id: vars
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          else
            echo "version=main-SNAPSHOT" >> "$GITHUB_OUTPUT"
          fi

      - name: Trigger JitPack build
        run: |
          VERSION="${{ steps.vars.outputs.version }}"
          URL="https://jitpack.io/com/github/MCAerogel/Aerogel/api/${VERSION}/api-${VERSION}.pom"
          HTTP_CODE="$(curl --silent --show-error --location \
            --connect-timeout 10 \
            --max-time 30 \
            --output /dev/null \
            --write-out "%{http_code}" \
            "$URL" || true)"

          if [[ "$HTTP_CODE" == "200" || "$HTTP_CODE" == "404" ]]; then
            echo "JitPack trigger request accepted (HTTP $HTTP_CODE): $URL"
          else
            echo "JitPack trigger request returned HTTP $HTTP_CODE (non-blocking): $URL"
          fi
